{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "if [ -n \"$DATABASE_URL\" ]; then npm run db:migrate; else echo 'DATABASE_URL is not configured; skipping Vercel database migration for this deployment.'; fi && npm run build",
  "git": {
    "deploymentEnabled": {
      "internal/*": false
    }
  }
}
